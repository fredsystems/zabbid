# Zabbid Environment Configuration Example
# Copy this file to .env and update with your actual values

# MariaDB Configuration
MYSQL_ROOT_PASSWORD=changeme_root_password
MYSQL_PASSWORD=changeme_zabbid_password

# Backend Configuration
JWT_SECRET=changeme_generate_a_secure_random_string_minimum_32_characters
RUST_LOG=info

# UI Configuration (used during build)
VITE_API_URL=/api

# Security Notes:
# - DO NOT commit the actual .env file to version control
# - Generate a strong random JWT_SECRET (e.g., using: openssl rand -base64 32)
# - Use strong, unique passwords for database credentials
# - Change all default values before deploying to production
